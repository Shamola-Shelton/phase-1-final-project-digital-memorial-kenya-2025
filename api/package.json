{
  "name": "kenya-memorial-api",
  "version": "1.0.0",
  "description": "API for Kenya Protest Memorial",
  "scripts": {
    "start": "json-server --watch api/db.json --port 10000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
